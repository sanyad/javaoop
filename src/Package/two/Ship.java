package Package.two;

public class Ship extends Vehicle {
    public Ship(int height, int count, int speed, int year) {
        setHeight(height);
        setCount(count);
        setSpeed(speed);
        setYear(year);
    }
}
